# Проверка версии CareerAI — 19 сентября 2026

## Подтверждено локально

- Java 21: 233 теста, 0 failures, 0 errors, 0 skipped, включая 6 интеграционных проверок PostgreSQL/HTTP.
- Собран исполняемый Spring Boot JAR. При упаковке сначала мешал запущенный preview-процесс Windows; после его остановки упаковка прошла успешно без изменения исходников.
- Чистая PostgreSQL 17: начальная схема B12 и миграции V13–V15.
- Реальный исходный дамп восстановлен в отдельную базу: 20 публикаций, версия V12. Обновление копии до V15 и 5 HTTP-интеграционных сценариев прошли; исходные базы не обновлялись.
- Проверены подпись и срок Telegram initData, allowlist, запрет доступа без авторизации, лимит тела запроса (включая chunked), аудит и конфликты одновременных правок.
- Жизненный цикл embeddings проверен на настоящем PostgreSQL с тестовым провайдером: индексирование, отсутствие лишнего вызова, изменение, сохранение retry/backoff, повтор, деактивация и восстановление.
- Браузер Chrome: отказ без Telegram-подписи, вход с подписанными тестовыми данными отдельного тестового токена, все разделы, FAQ create/deactivate, архивирование/восстановление, desktop 1440 px и mobile 390 px. Нет ошибок JavaScript и горизонтального переполнения страницы.
- Проверка исходников не обнаружила настроенных секретов. Дампы и конфигурация IntelliJ хранятся отдельно от репозитория.

## Особенности локального окружения

Ограниченная Windows-сессия не позволяла javac разрешить родительские каталоги при закрытии ZIP classpath. Использован временный внешний адаптер настоящего javac 21: он сохранял ошибки/exit code, снимал directory classpath в JAR и обходил только ошибку закрытия ZIP после успешной компиляции. Адаптер проверен на заведомо ошибочном Java-коде. Параметры проекта и production-сборки не менялись ради этого обхода. Для Java NIO задан короткий временный каталог Unix-domain sockets. Эти локальные файлы не входят в репозиторий.

CI выполняет обычный Maven verify на Linux/Java 21 с отдельным PostgreSQL 17, без Windows-адаптера. Результат каждого удалённого запуска следует проверять в GitHub Actions.

## Что ещё требует внешнего подключения

- Публичный HTTPS-адрес Mini App и реальные Telegram user ID администраторов ещё не заданы.
- Работа новой версии с живым Telegram-каналом и настоящими Gemini/Groq API не проверялась: при проверках polling и внешние фоновые вызовы отключены.
- Нет обещания конкретного времени ответа AI. Тесты подтверждают сокращение вызовов на быстрых путях и корректность fallback; фактическая задержка зависит от провайдера.
- Запуск на постоянном сервере не выполнялся. Подготовка и переключение описаны в docs/deployment.md.
- Анализ резюме сознательно отложен.
